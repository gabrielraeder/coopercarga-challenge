version: '3.9'
services:
  frontend:
    build: ./
    ports:
      - 5173:5173
    platform: linux/x86_64
    working_dir: /app-frontend
    healthcheck:
      test: ["CMD", "lsof", "-t", "-i:5174"] 
      timeout: 10s
      retries: 5